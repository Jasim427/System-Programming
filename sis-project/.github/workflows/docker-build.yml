name: Docker Build

on:
  workflow_dispatch:
  push:
    branches: [ Develop ]

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t sis-app .
